# L3 实机记录 (field runs)

S100P 现场 session 的证据笔记：P0 验证结果、发现的 bug、rosbag 路径、下一 session 待办。

**命名**：`YYYY-MM-DD.md`（与 BACKLOG 里 `Last on robot` 日期对齐）

**何时写**：

1. 周五 L3 P0 脚本跑完，或任意一次有意义的真机长跑后
2. BACKLOG 条目从 `doing` → `verified (robot)` 时，在本目录补一条链接

**相关**：

- P0 脚本：`docs/07-testing/p0_*.sh`
- 回归层级说明：`docs/07-testing/README.md` § L3
- BACKLOG 单一真源：`docs/archive/01-project/BACKLOG.md`

> 公司级跨项目笔记仍放 `D:\inovxio\vault\`；**LingTu 真机验收证据**统一放本目录，便于 PR / BACKLOG 直接链接。
