# LingTu core portable dependencies.
#
# This is the smallest Python compatibility layer intended to install on both
# Windows and Ubuntu/Linux without ROS2, colcon, Gateway, ML/perception, LLM,
# vendor robot drivers, or MuJoCo simulation packages.
#
# For the MuJoCo desktop simulation target, install requirements-sim-mujoco.txt.
# Thunder Lite currently mirrors this set in requirements-lite.txt for backwards
# compatibility with existing deployment docs.

numpy>=1.24,<2.0
scipy>=1.10
PyYAML>=6.0
pydantic>=2.0
