schema: lingtu.sim.sensor-package.v1
id: imu
version: 1.0.0
kind: sensor
description: Six-axis inertial measurement unit contract.
sensor_type: imu
outputs:
  - angular_velocity
  - linear_acceleration
timing:
  rate_hz: 500
interface:
  transport: native_typed_dds
  message_type: lingtu.sim.imu.v1
