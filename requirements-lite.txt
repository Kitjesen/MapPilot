# LingTu Thunder Lite dependencies.
#
# This is the no-ROS, no-Gateway, no-semantic-model install target for
# `python lingtu.py thunder-lite` local smoke deployment.
# It intentionally mirrors requirements-core-portable.txt while Thunder Lite
# deployment docs are migrated to the generic portable core package.
#
# Real Thunder hardware control also needs:
#   pip install -e '.[lite,thunder]'

numpy>=1.24,<2.0
scipy>=1.10
PyYAML>=6.0
pydantic>=2.0
