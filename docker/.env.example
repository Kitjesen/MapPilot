# ═══════════════════════════════════════════════════════════════
# 3d_NAV Docker Environment Variables
# 复制为 .env 并修改:  cp docker/.env.example .env
# ═══════════════════════════════════════════════════════════════

# ── 版本 ──
NAV_VERSION=1.3.0

# ── 子系统开关 (true/false) ──
ENABLE_LIDAR=true
ENABLE_SLAM=true
ENABLE_AUTONOMY=true
ENABLE_PLANNING=true
ENABLE_OTA=true

# ── LiDAR 网络 ──
SETUP_LIDAR_NET=true
LIDAR_INTERFACE=eth0
LIDAR_HOST_IP=192.168.1.5

# ── 规划参数 ──
NAV_MAP_PATH=
NAV_INIT_X=0.0
NAV_INIT_Y=0.0
NAV_INIT_Z=0.0
NAV_INIT_YAW=0.0

# ── ROS2 ──
ROS_DOMAIN_ID=0

# ── 宿主机路径映射 ──
HOST_MAP_DIR=./maps
HOST_PLANNING_ENV=/dev/null

# ── 开发用 (docker-compose.dev.yml) ──
HOST_UID=1000
HOST_GID=1000
CMAKE_BUILD_TYPE=RelWithDebInfo

# ── X11 转发 (rviz2, rqt) ──
DISPLAY=:0
