- **开发启动**: 执行 `npm run dev` 启动 Vite 开发服务器（默认端口 3000）。
- **环境变量**: 可通过设置 `ROBOT_HOST` 环境变量指定后端网关地址（如 `ROBOT_HOST=192.168.1.100:5050 npm run dev`）。
- **健康检查**: 提供 `npm run smoke:gateway` 和 `npm run smoke:dataflow-ui` 脚本用于验证网关连通性及 UI 数据契约。