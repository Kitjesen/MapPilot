- 所有仿真引擎实现必须继承自 `sim.engine.core.SimEngine` 并实现 `load`、`step`、`reset` 等标准生命周期方法。
- 传感器数据读取（如 `get_lidar_points`）统一返回 NumPy 数组，且坐标帧默认遵循世界坐标系或 ROS 约定。
- 场景资源路径解析优先通过 `WorldRegistry` 或 `RobotConfig` 进行标准化处理，避免在引擎内部硬编码文件路径。