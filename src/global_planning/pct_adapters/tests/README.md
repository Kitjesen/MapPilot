# PCT Planner Adapter Tests

Placeholder for PCT global planner adapter tests. Currently empty — adapters are tested indirectly through global_planning integration tests.

```bash
python -m pytest src/global_planning/pct_adapters/tests/ -q
```

No special markers required.
