# PCT Planner Tests

Helper scripts and map validation for PCT global planner. Includes check_map.py for testing planner routing on saved maps.

```bash
python -m pytest src/global_planning/pct_planner/tests/ -q
```

Markers: `ros2` — requires ROS2 runtime for launch-based tests.
