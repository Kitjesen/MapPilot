# PCT Planner Launch Tests

Tests for PCT planner ROS2 launch configurations: planner_only_launch.py and test_planning_launch.py.

```bash
python -m pytest src/global_planning/pct_planner/launch/test/ -q
```

Markers: `ros2` — requires ROS2 runtime for launch file testing.
