# Global Planning Package Tests

Import and contract tests for the global planning package (AStar and PCT backends).

```bash
python -m pytest src/global_planning/tests/ -q
```

No special markers required.
