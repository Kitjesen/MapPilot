# Core Framework Tests

Unit tests for the LingTu module framework: Module, Blueprint, Stream, Registry, Transport, and utility modules. Includes 1226+ tests that run on any machine (no ROS2 or hardware needed).

```bash
python -m pytest src/core/tests/ -q
```

No special markers required. The primary test suite for framework correctness.
