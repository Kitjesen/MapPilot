# Exploration Module Tests

Contract tests for TARE exploration modules: ExplorationSupervisorModule and TareExplorerModule.

```bash
python -m pytest src/exploration/tests/ -q
```

Markers: `ros2` — tests requiring ROS2 runtime for exploration ROS2 bridge.
