# Semantic Package Tests

Top-level import-verification tests for the semantic package (perception, planner, reconstruction sub-packages).

```bash
python -m pytest src/semantic/tests/ -q
```

No special markers required.
