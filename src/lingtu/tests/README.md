# LingTu Package Tests

Import-verification tests for the top-level `lingtu` package and its re-exported functional API.

```bash
python -m pytest src/lingtu/tests/ -q
```

No special markers required.
