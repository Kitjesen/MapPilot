# Gateway Package Tests

Tests for the HTTP/WS/SSE API gateway: app bootstrap, commands, helpers, runtime status, and MCP auth.

```bash
python -m pytest src/gateway/tests/ -q
```

No special markers required.
