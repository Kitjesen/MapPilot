# Sim camera deps

Simulation camera dependencies live here when a backend needs external assets
or libraries. The current MuJoCo camera backend reads from the existing engine
and does not need a vendored package.
