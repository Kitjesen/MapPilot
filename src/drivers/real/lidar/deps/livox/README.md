# Livox deps

Vendored no-ROS Livox dependencies live here.

```text
Livox-SDK2/
```

`Livox-SDK2` is the product SDK path. The former ROS2 Livox fallback was
removed from the product repository; integration experiments belong outside
the ROS-free runtime tree.
