# Base Autonomy Tests

Contract tests for terrain, local planner, and path follower modules and their composable stack.

```bash
python -m pytest src/base_autonomy/tests/ -q
```

No special markers required.
