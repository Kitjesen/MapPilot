"""Perception domain package.

This package owns scene perception modules, detectors, encoders, tracking,
scene-graph construction, and reconstruction helpers. External protocol code
belongs under ``perception.adapters``.
"""
