"""Perception domain package.

This package owns scene perception modules, detectors, encoders, tracking,
scene-graph construction, and reconstruction helpers. The Blueprint boundary
is ``perception.module``; backend construction is centralized in
``perception.backends``.
"""
